apiVersion: v1
kind: Service
metadata:
  name: {{ include "zm-scpro-message-receiver.fullname" . }}
  labels:
    {{- include "zm-scpro-message-receiver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zm-scpro-message-receiver.selectorLabels" . | nindent 4 }}
