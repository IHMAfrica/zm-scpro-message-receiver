apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zm-scpro-message-receiver.fullname" . }}-test-connection"
  labels:
    {{- include "zm-scpro-message-receiver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zm-scpro-message-receiver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
